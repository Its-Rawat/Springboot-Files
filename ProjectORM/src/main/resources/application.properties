spring.application.name=ProjectORM


spring.datasource.url=jdbc:mysql://localhost:3306/orm
spring.datasource.username=root
spring.datasource.password=springboot
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA/Hibernate Configurations

spring.jpa.hibernate.ddl-auto=update
#none: The default for MySQL. No change is made to the database structure.
#update: Hibernate changes the database according to the given entity structures.
#create: Creates the database every time but does not drop it on close.
#create-drop: Creates the database and drops it when SessionFactory closes.


# Through this we can see logs of which query the Hibernate is firing
spring.jpa.show-sql=true


# dialect: Hibernate will make query for the database type like mysql,oracle ...
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
